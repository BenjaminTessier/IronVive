using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RayViewer : MonoBehaviour {

    private float weapondRange = 1000F;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        Vector3 lineOrigin;// = ...;
        //Debug.DrawRay(lineOrigin, transform.forward * weapondRange, Color.green);
	}
}
